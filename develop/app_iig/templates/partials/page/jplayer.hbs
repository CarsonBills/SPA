
$('.jp-jplayer').jPlayer({
	ready: function () {
	  	$(this).jPlayer('setMedia', {
	  		m4v: '{{{videoSrc}}}',
	    	poster: '{{{stillImageSrc}}}'
	  	});
	},
	autoBlur: true,
	size: {
		width: 'auto',
		height: 'auto'
	},
	useStateClassSkin: true,
	preload: 'meta',
	supplied: 'm4v',
	cssSelectorAncestor: '.video-container',
	cssSelector: {
  	}
});

/*$('.jp-jplayer').click(function (e) {
	var $jp = $(e.currentTarget);
	if ($('.video-container').hasClass('jp-state-playing')) {
		$jp.jPlayer('pause');
	} else {
		$jp.jPlayer('play');
	}
	return false;
});*/