<!DOCTYPE html>
<html lang="en">
	<head>
    	<meta charset="utf-8">
    	<title>{{title}}</title>
      <style type="text/css">
          .container { width: 100%; }
          table {border-collapse: collapse; table-layout: fixed; margin: 0 auto; cellpadding: 0; cellspacing: 0;}
          caption h2 {font-family: sans-serif; padding: 10px 0;}
          thead tr {background-color: #aaa;}
          thead tr, tbody {border: 1px solid red;}
          tbody tr td, thead tr td {padding: 10px;}
          tbody tr {background-color: #eee;}
          tbody tr:nth-child(2n+1) {background-color: #fff; padding: 10px;}
      </style>
  </head>
  <body>
    <div class="container">
      	<table>
            <caption><h2>{{title}}</h2></caption>
            <thead>
      		    <tr>
        		 	{{#each legend}}
                    <td>{{this}}</td>
                    {{/each}}
                </tr>
            </thead>
            <tbody>
                {{#each entries}}
                    <tr>
                        {{#each this.entry}}
                        <td>{{this}}</td>
                        {{/each}}
                    </tr>
                {{/each}}
            </tbody>
      	</table>
    </div>
  </body>
</html>